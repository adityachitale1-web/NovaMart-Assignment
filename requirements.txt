# Navigate to your project folder
cd path/to/novamart-assignment

# Create/update requirements.txt
echo "streamlit==1.31.0
pandas==2.1.4
plotly==5.18.0
numpy==1.26.3
openpyxl==3.1.2" > requirements.txt

# Add, commit and push
git add requirements.txt
git commit -m "Fix: Add missing plotly dependency"
git push origin main
